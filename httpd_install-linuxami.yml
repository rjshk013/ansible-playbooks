---
- name: This sets up an httpd webserver
  hosts: aws-ec2
  tasks:
  - name: Install apache packages 
    yum:
      name: httpd
      state: present
  - name: ensure httpd is running
    service:
      name: httpd 
      state: started
#need to change python3 to python interpreter
